<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Spring Boot Parent -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <!-- 主 POM（聚合模块，不构建任何产物） -->
    <groupId>com.dualenigma</groupId>
    <artifactId>server-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>DualEnigma Server Parent</name>
    <description>DualEnigma 服务端多模块聚合 POM</description>

    <!-- 子模块 -->
    <modules>
        <module>network</module>
        <module>game-server</module>
        <module>account-server</module>
    </modules>

    <!-- 全局属性 -->
    <properties>
        <java.version>21</java.version>
        <mariadb.version>3.4.1</mariadb.version>
    </properties>

    <!-- 全局依赖管理（子模块按需引用，不需写版本号） -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.mariadb.jdbc</groupId>
                <artifactId>mariadb-java-client</artifactId>
                <version>${mariadb.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
